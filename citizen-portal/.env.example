# Optional. Where the Vite dev-server proxy forwards /api requests.
# Only needed if the backend isn't on http://localhost:5051 (e.g. inside
# docker compose it's set to http://backend:5051). Not used by the
# production build -- nginx proxies /api/v1/track there.
# VITE_API_PROXY_TARGET=http://localhost:5051
